//! Minimal test harness placeholder for hydra-axiom.
//! The combined harness lives in hydra-prediction.

fn main() {
    println!("hydra-axiom test harness — use hydra-prediction for the combined harness");
}
