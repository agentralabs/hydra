//! Placeholder test harness for hydra-trust.
//! The combined harness lives in hydra-adversary.

fn main() {
    println!("hydra-trust test harness — see hydra-adversary for the combined harness");
}
