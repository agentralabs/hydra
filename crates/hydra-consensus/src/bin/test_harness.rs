//! Phase 39 test harness — hydra-consensus only.
//! Run: cargo run -p hydra-consensus --bin test_harness

fn main() {
    println!("hydra-consensus test harness — use combined harness in hydra-consent");
}
